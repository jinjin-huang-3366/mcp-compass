interface:
  display_name: "MCP Task PR Flow"
  short_description: "Implement and publish one MCP Compass task PR"
  default_prompt: "Use $mcp-task-pr-flow to implement one MCP Compass task locally, then validate it and open a pull request with an email summary without merging."
